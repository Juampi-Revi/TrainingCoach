/**
 * Constantes de negocio para el módulo de Training.
 * Re-exporta desde lib/constants.ts que es la fuente centralizada.
 */
export {
  MUSCLE_LABEL,
  GROUP_COLORS,
  GROUP_LETTERS,
  groupLabel,
  BLOCK_TYPE_LABELS,
  INTERVAL_TYPE_LABELS,
  blockTypeLabel,
  blockSummary,
  fmtDuration,
  BLOCK_COLORS,
  BLOCK_ICONS,
} from "@/lib/constants";

export type { BlockType, IntervalType, WorkoutBlockSummary } from "@regen/types";
