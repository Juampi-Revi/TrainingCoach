# Dependencies
node_modules
.pnpm-store

# Build outputs
.next
out
dist
build

# Env files
.env
.env.local
.env.*.local

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# OS
.DS_Store

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Prisma generated
apps/api/node_modules/.prisma

# Local DB backups (never commit)
backups/
*.dump
*.sql

# Prisma migrations (must be committed)
!apps/api/prisma/migrations/**/migration.sql

# AI / IDE tooling
.claude
.agents
.trae
skills-lock.json

# Design archaeology (never re-commit)
panel-mockups*
Training App*/
docs/assets/

# Accidental nested app copies (not real Prisma migrations)
apps/web/apps/
apps/api/apps/
